use serde::{Serialize, Deserialize};
use serde_json::{Result, Value};

#[derive(Debug, Clone)]
pub struct Handler {
    
}

/* supposed to handle the searching of the lists */
impl Handler {
    fn new() -> Self {
        Self {

        }
    }

    
}